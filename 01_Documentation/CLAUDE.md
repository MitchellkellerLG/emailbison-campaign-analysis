# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

This workspace is currently empty and does not contain an active codebase. 

When a project is initialized in this directory, please update this file with:
- Build, test, and lint commands specific to the project
- High-level architecture and design patterns used
- Key dependencies and their purposes
- Project-specific conventions and practices

## Current Configuration

The workspace has Claude CLI configuration in `.claude/settings.local.json` that permits:
- Web fetching from docs.anthropic.com
- Running `ls` bash commands